void PrintMenu(void) {
  Serial.println(F("a = test IR LED"));
  Serial.println(F("b = test"));
  Serial.println(F("c = buttonTestSketch"));
  Serial.println(F("z = print menu"));
  Serial.println(F("d = TestMotor"));
  Serial.println(F("e = irSensorValue"));
  Serial.println(F("f = Test encoder and motor"));
  Serial.println(F("g = Press g to count IR stripes"));
  Serial.println(F("h = Move launcher to desired location"));
  Serial.println(F("i = test aim fire"));
  Serial.println(F("j = reloader"));
  Serial.println(F("k = Demo1"));
  Serial.println(F("s = get data from MATLAB"));
  Serial.println(F("t = targeting"));
  Serial.println(F("v = Demo4"));
}